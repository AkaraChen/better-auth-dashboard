import type { Config } from "./dashboard.config.type";

const config: Config = {
    brand: 'Better Auth Dashboard',
    features: ['admin', 'organizations', 'api-key'],
    customRoles: ['teacher'],
}

export default config;
