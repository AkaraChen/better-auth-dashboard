1. 遇到不熟悉的或者用法不太确定的库，就用 context7 来查询文档。
2. 在编写完成后，运行 `pnpm tsc --noEmit`，不要尝试运行或者编译项目。
3. 如果有不明白的请直接询问用户
4. 只使用 better-auth 的 client 功能，不要使用任何 better-auth 的 server 功能。
5. 除非极其边缘的情况，请求 API 要使用 tanstack query。
